// boilerplate for exports

module.exports = {
    Mutation: { 
        async createMessage(_, {messageInput:{text,username}}){
            
            
        }
    },
    Query: {
        //message: (_, {ID}) => Message.findById(ID)
    }
}